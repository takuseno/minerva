#!/bin/bash -eux

# create temporary directory for tests
mkdir -p test_data

# run tests
echo "Run unit tests"
pytest --cov-report=xml --cov=ganglion tests -p no:warnings -v

# clean up
rm -r test_data

# build javascript
npm run build
